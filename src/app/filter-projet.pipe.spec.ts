import { FilterProjetPipe } from './filter-projet.pipe';

describe('FilterProjetPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProjetPipe();
    expect(pipe).toBeTruthy();
  });
});
